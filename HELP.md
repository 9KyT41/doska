# ДОСКА — легенда управления (альфа)

Коротко: мысль ловится голосом (зажал пилюлю/Пробел — сказал — отпустил),
клавой (N — напечатал — Enter) или вставкой (Ctrl+V). Всё остальное — фаза
сборки: двигать, связывать нитками, группировать в блоки, экспортировать.

## Захват мысли
| Действие | Клавиши / мышь |
|---|---|
| Новая пустая мысль, сразу в правку | `N` (РУ: `т`) |
| Голос: запись | зажать `Пробел` или пилюлю 🎙; отпустить — стоп |
| Вставить текст | `Ctrl+V` |
| Вставить картинку | `Ctrl+V` (если в буфере изображение) |
| Картинка файлом | перетащить: на доску = новая нода; на ноду = добавить часть |

## Правка текста
| Действие | Клавиши |
|---|---|
| Править текст выделенной ноды | `Enter` |
| Править конкретную часть | dblclick по тексту / названию ноды |
| Из заголовка в текст | `Enter` (в заголовке) |
| Выйти из правки (сохранить) | `Enter` (в тексте) или клик мимо |
| Выйти из правки (закрыть) | `Esc` |

## Выделение и удаление
| Действие | Клавиши / мышь |
|---|---|
| Выделить ноду | клик |
| Добавить/убрать в выделении | `Ctrl+клик` |
| Выделить ноды блока | клик по телу рамки блока |
| Снять выделение | клик по пустому месту |
| Удалить выделенное (в корзину) | `Delete` |
| Вернуть последнее из корзины | `Ctrl+Z` (РУ: `я`) |

## Доска и вид
| Действие | Клавиши / мышь |
|---|---|
| Пан | тянуть пустое место левой кнопкой |
| Зум к курсору | колесо мыши |
| Вписать все ноды | `F` (РУ: `а`) |
| Сброс вида к 100% | dblclick по пустому месту |

## Ноды
| Действие | Мышь |
|---|---|
| Переместить ноду | тянуть за ноду (или грип ⠿) |
| Вынуть ноду из блока | `Alt` + тянуть ноду |

## Граф (нитки)
| Действие | Мышь |
|---|---|
| Связь «до / после / вместе / доп» | тянуть из гнезда в другую ноду |
| Удалить связь | dblclick по нитке |

## Блоки
| Действие | Клавиши / мышь |
|---|---|
| Создать блок (лассо) | зажать `B` (РУ: `и`) + тянуть рамку |
| Переименовать блок | dblclick по шапке; `Enter` — готово, `Esc` — отмена |
| Цвет блока | клик по точке на шапке → палитра |
| Удалить блок (ноды остаются) | клик по `×` на шапке |
| Переместить блок целиком | тянуть за шапку |

## Проект
| Действие | Клавиши |
|---|---|
| Сохранить | `Ctrl+S` (РУ: `ы`) |
| Создать / открыть / сохранить как / переименовать / экспорт | меню `☰` |

## Честные примечания
- `Ctrl+Z` в альфе = **вернуть из корзины**, а не общий undo. Общий undo — в планах.
- Блоки = главы в экспорте. Приоритет секций: блоки с нитками → свободные цепочки →
  блоки без ниток → остальное. Внутри блока/цепочки порядок по ниткам; par = ансамбль.
- Экспорт «Чистый текст» игнорирует картинки и берёт у аудио его расшифровку;
  результат пишется в `.txt` и кладётся в буфер обмена.